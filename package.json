{
  "name": "ha-codex-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Home Assistant add-on repository for HA_Codex_UI.",
  "license": "MIT",
  "scripts": {
    "ci:remote-only": "echo \"Use GitHub Actions for execution in this project.\" && exit 2"
  }
}
