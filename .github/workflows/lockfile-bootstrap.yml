name: Lockfile Bootstrap

on:
  workflow_dispatch:
  push:
    paths:
      - ha_codex_ui/backend/package.json
      - ha_codex_ui/frontend/package.json

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: lockfile-bootstrap-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lockfiles:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Generate backend lockfile remotely
        working-directory: ha_codex_ui/backend
        run: npm install --package-lock-only
      - name: Generate frontend lockfile remotely
        working-directory: ha_codex_ui/frontend
        run: npm install --package-lock-only
      - name: Audit generated locks
        run: |
          (cd ha_codex_ui/backend && npm audit --audit-level=high)
          (cd ha_codex_ui/frontend && npm audit --audit-level=high)
      - uses: actions/upload-artifact@v4
        with:
          name: generated-package-locks
          path: |
            ha_codex_ui/backend/package-lock.json
            ha_codex_ui/frontend/package-lock.json
      - name: Create lockfile PR
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "Update generated package lockfiles"
          title: "Update HA_Codex_UI package lockfiles"
          body: "Generated by GitHub Actions because local package manager execution is disabled for this project."
          branch: actions/update-lockfiles
