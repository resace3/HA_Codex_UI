name: Release Build

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  packages: write
  id-token: write
  security-events: write

concurrency:
  group: release-build-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - name: Validate and test
        run: |
          python3 scripts/validate-addon-metadata.py
          bash scripts/check-name-collisions.sh
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/resace3/ha-codex-ui
      - name: Build and publish multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: ha_codex_ui
          file: ha_codex_ui/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: Trivy image scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ghcr.io/resace3/ha-codex-ui:latest
          format: sarif
          output: trivy-release-image.sarif
          version: v0.72.0
      - uses: github/codeql-action/upload-sarif@v3
        if: always()
        continue-on-error: true
        with:
          sarif_file: trivy-release-image.sarif
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-release-image-sarif
          path: trivy-release-image.sarif
          if-no-files-found: ignore
      - name: Release summary
        run: |
          echo "### HA_Codex_UI release" >> "$GITHUB_STEP_SUMMARY"
          echo "Published GHCR image ghcr.io/resace3/ha-codex-ui when the release workflow succeeded." >> "$GITHUB_STEP_SUMMARY"
