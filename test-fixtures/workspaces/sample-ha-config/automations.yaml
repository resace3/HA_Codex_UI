- id: ha_codex_ui_fixture_light
  alias: HA_Codex_UI fixture automation
  trigger:
    - platform: state
      entity_id: input_boolean.ha_codex_ui_fixture
      to: "on"
  action:
    - service: logbook.log
      data:
        name: HA_Codex_UI
        message: Fixture automation triggered.
